CREATE DEFINER=`root`@`localhost` PROCEDURE `atualizar_conta_poupanca`(
		IN p_id_conta BIGINT,
        IN p_taxa_rendimento DOUBLE
        )
BEGIN
		UPDATE conta_poupanca
		SET taxa_rendimento = p_taxa_rendimento
		WHERE id_conta = p_id_conta;
END